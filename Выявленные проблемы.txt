1) QuestBase
    При чтении пустого файла никак на это не реагирует, temperTest стартует и выводит, что пользователь флегматик.
    Конструкторы не тестируются.
2) Test
    Как тестировать метод, который ничего не принимает и ничего не возвращает?